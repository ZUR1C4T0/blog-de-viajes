<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mis Publicaciones</title>
    <link rel="shortcut icon" href="/img/favicon.ico" type="image/x-icon">

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css">
</head>

<body class="d-flex flex-column" style="min-height: 100vh;">
    <%- include("./partials/private_header.ejs", { active: "admin" , session }) %>

        <div class="container-md flex-grow-1 d-flex flex-column">
            <div class="row">
                <div class="col mt-3">
                    <h1>Mis Publicaciones</h1>
                    <hr>
                </div>
            </div>
            <div class="row">
                <div class="col table-responsive">
                    <table class="table table-bordered table-hover">
                        <thead class="table-dark">
                            <tr>
                                <th>TITULO</th>
                                <th>RESUMEN</th>
                                <th>VOTOS</th>
                                <th>ACCIONES</th>
                            </tr>
                        </thead>
                        <tbody>

                            <% publicaciones.forEach(publicacion=> { %>
                                <tr>
                                    <td class="text-capitalize">
                                        <%= publicacion.titulo %>
                                    </td>
                                    <td>
                                        <%= publicacion.resumen %>
                                    </td>
                                    <td>
                                        <%= publicacion.votos %>
                                    </td>
                                    <td>
                                        <a href="/admin/publicaciones/editar/<%= publicacion.id %>"
                                            class="btn btn-warning">
                                            <i class="fa fa-pen-to-square"></i>
                                        </a>
                                        <a href="/admin/publicaciones/eliminar/<%= publicacion.id %>"
                                            class="btn btn-danger">
                                            <i class="fa fa-trash"></i>
                                        </a>
                                    </td>
                                </tr>
                                <% }) %>

                        </tbody>
                    </table>
                </div>
            </div>
            <div class="row justify-content-end mt-auto m-3 mx-md-0 sticky-bottom" style="bottom: 20px;">
                <a href="/admin/publicaciones/agregar" class="btn btn-primary btn-lg rounded-circle mt-3"
                    style="padding: 8px 12px; width: 48px; height: 48px; right: 20px; ">
                    <i class="fa fa-file-circle-plus"></i>
                </a>
            </div>
        </div>

        <%- include("./partials/footer") %>
</body>

</html>